# overloading
# funtion signature is no of aarguments and types of arguments
# so each function have different numbers of arguments and types of arguments
# len funtion it take slen("deepak") and also len(10) so len functon is overloaded
# it is also called compile time polymorphism




#OVERIDING ALSO CALLED AS OVERSHODOWING
# if two classes base and derived class and have same methods so overriding the function in derived calss is called
# overriding
#it is also called runtime polymorphism
# in this in derived we derived the same funtion in child calss

